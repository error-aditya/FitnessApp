import 'package:flutter/material.dart';

class NoTransitionExample extends StatelessWidget {
  const NoTransitionExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}